name: main

on:
  push:
    branches:
      - main

jobs:
  build:
    name: Build docs
    uses: ./.github/workflows/build.yml
  publish:
    name: Publish docs
    uses: ./.github/workflows/publish.yml
    needs: build
